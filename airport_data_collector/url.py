from django.conf.urls import url
#from .views import ListAssetView, AssetDetailView, upload_data

# urlpatterns = [
#     url('assets/list', ListAssetView.as_view(), name='assets-all'),
#     url('assets/', ListAssetView.as_view(), name='assets-all'),
#     url(r'^(?P<asset_id>[\w-]+)/$', AssetDetailView.as_view(), name="asset-detail"),
#
# ]
